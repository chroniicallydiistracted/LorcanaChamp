# Decklist Fixtures

This directory is reserved for Lorcanito-derived real-card decklist fixtures.

Decklist fixtures must not use synthetic cards. Convert them to `CardsMaps` before using them as runtime static-resource fixtures.

